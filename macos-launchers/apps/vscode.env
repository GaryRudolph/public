# Source app for the VS Code wrapper. NAME here = app display name
# (wrapper output is "<NAME> <LABEL>.app" where LABEL comes from profile env).
# NAME and SRC are quoted because both contain spaces; the Makefile sources
# this file via `.` (dot) so unquoted spaces would be parsed as command args.
NAME="Visual Studio Code"
SRC="/Applications/Visual Studio Code.app"
