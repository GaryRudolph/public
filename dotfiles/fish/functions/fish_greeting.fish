# Override the default fish greeting with a no-op so new shells start silent.
# Fish autoloads this from `functions/` and prefers it over the shipped one in
# /opt/homebrew/share/fish/functions/fish_greeting.fish.
function fish_greeting
end
